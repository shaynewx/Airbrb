import React from 'react';


function PageList () {
    const [token, setToken] = React.useState(null);
    // const logout = () => {
    //   setToken(null);
    //   localStorage.removeItem('token');
    // }
    return (
        <>
        <Router>
            <PageList />
        </Router>
        </>
    );
}